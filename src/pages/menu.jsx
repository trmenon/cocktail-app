import React from 'react';

import {Search, Canvas} from '../components';

export const Menu = ()=> {
    return(
        <React.Fragment>
            <Search/>
            <Canvas/>
        </React.Fragment>
    )
}